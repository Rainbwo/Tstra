# version
cmake_minimum_required(VERSION 3.0)

# project
project(Tstra_mex)

aux_source_directory(. DIR_SRCS)

set(Tstra_VERSION_MAJOR 1)
set(Tstra_VERSION_MINOR 0)

set(MATLAB_ROOT "D:\\Program Files\\MATLAB\\R2017a\\extern")
include_directories(${MATLAB_ROOT}/include)
link_libraries(${MATLAB_ROOT}/lin/win64/mingw64)


# library
add_library(Tstra_mex $(DIR_SRCS))

